# Cargo Commands

1. `cargo new ***` - This creates a new cargo project.
2. `cargo build` - Compiles the cargo project and creates an executable in the `target` directory which you would have to run separately.
3. `cargo run` - Allows you to build and run the project at once.
4. `cargo check` - Build a project without producing an executable to check for errors.
