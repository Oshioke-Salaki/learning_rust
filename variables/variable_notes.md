# Points

- By default variables are immutable
- Adding a `mut` keyword before the variable name makes it mutable
- Use upper snake case when defining constants.
- When creating a constant, you must declare their type
- Constants can be used globally
